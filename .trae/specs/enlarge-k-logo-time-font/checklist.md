# Checklist

- [ ] Header.tsx 中 K 字 `span` 字号为 `text-3xl`（30px）
- [ ] Header.tsx 中点号 `span` 字号为 `text-3xl`（30px）
- [ ] Header.tsx 中 '导航' `span` 字号为 `text-xl`（20px）
- [ ] Header.tsx 中时间 `span` 字号为 `text-lg`（18px）
- [ ] Header.tsx 保持 `h-18` 高度与 `fixed top-0` 固定定位
- [ ] Header.tsx 保持 `flex items-center justify-between h-full` 垂直居中
- [ ] Header.tsx 保持 K 字 font-family、颜色 #6BC5E8、text-shadow 发光效果不变
- [ ] Header.tsx 保持左右内边距 `px-4 sm:px-6 lg:px-8` 不变
- [ ] TypeScript 编译通过（`npx tsc --noEmit` 无错误）
- [ ] 变更已提交并推送到 origin/main
