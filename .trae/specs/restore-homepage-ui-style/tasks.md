# Tasks

- [x] Task 1: 优化首页UI样式
  - [ ] SubTask 1.1: 将整个内容区域用统一的白色容器包裹（bg-white rounded-2xl shadow-sm border border-gray-200 p-8），去除内部元素多余的白色重复背景
  - [ ] SubTask 1.2: 优化统计卡片：保留现有样式的基础上，增加 hover 效果（hover:shadow-md + hover:-translate-y-0.5），三列间距调整为 gap-4
  - [ ] SubTask 1.3: 优化搜索框：增加 subtle 背景和边框，focus 状态 ring 更明显
  - [ ] SubTask 1.4: 优化数据集卡片：增强边框和 hover 效果（hover:shadow-lg、hover:border-cyan-400/50），确保卡片圆角、边框、悬停效果清晰可见
  - [ ] SubTask 1.5: 优化空状态样式：让空状态卡片与整体风格一致
  - [ ] SubTask 1.6: 调整页面整体间距：让布局层次更清晰，避免显得光秃秃
  - [ ] SubTask 1.7: 运行 npx tsc --noEmit 验证编译，git commit 并 push

# Task Dependencies
- None（单一任务）
